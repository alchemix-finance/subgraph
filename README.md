# Alchemix Subgraph

## Quickstart

```bash
# Boot docker containers (wait until graph-node is ready)
docker-compose up -d

# Install node dependencies
yarn install

# Deploy subgraph
yarn codegen
yarn deploy
```
